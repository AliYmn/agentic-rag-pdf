# --- LLM + Embeddings (single provider: OpenAI) ---
openai>=1.50

# --- Vector store ---
chromadb>=0.5.0

# --- Retrieval (lexical) ---
bm25s>=0.2.0
PyStemmer>=2.2.0          # optional; better/faster BM25 stemming

# --- PDF / document processing ---
PyMuPDF>=1.24.0           # fitz: text extraction + page image rendering

# --- App / config ---
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
rich>=13.7.0              # nicer CLI output / logging

# --- Numerics (also pulled in transitively) ---
numpy>=1.26.0
